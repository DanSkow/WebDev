############## INFORMATIONAL ##############
# This iRule directs users to an HTML page from an iFile if the pool has 0 available members

############## SETUP ##############
# 1) Add an HTML file to System > File Management > iFile List

when HTTP_REQUEST {
 if { [active_members [LB::server pool]] < 1 } {
  HTTP::respond 503 content [ifile get maintenance-splash-page.html] }
}
